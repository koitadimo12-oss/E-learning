export type Chapitre = { titre: string; texte: string };
export type ContenuLivre = { chapitres: Chapitre[] };

const C: Record<string, ContenuLivre> = {
  "Introduction à l'Algorithmique": { chapitres: [
    { titre: "Qu'est-ce qu'un Algorithme ?", texte: "Un algorithme est une suite finie d'instructions permettant de résoudre un problème. Il doit être précis, déterministe et se terminer en un nombre fini d'étapes.\n\nExemple : trier une liste de nombres par ordre croissant est un problème algorithmique classique. L'algorithme de tri à bulles compare deux éléments adjacents et les échange si nécessaire, jusqu'à ce que la liste soit ordonnée.\n\nLes propriétés fondamentales d'un bon algorithme :\n1. Correction : il produit le bon résultat\n2. Terminaison : il s'arrête toujours\n3. Efficacité : il utilise peu de ressources" },
    { titre: "Complexité Algorithmique", texte: "La complexité mesure l'efficacité d'un algorithme via la notation O (Grand O).\n\n- O(1) : temps constant (accès tableau)\n- O(log n) : logarithmique (recherche binaire)\n- O(n) : linéaire (parcours liste)\n- O(n²) : quadratique (tri à bulles)\n- O(2ⁿ) : exponentiel (à éviter)\n\nLe choix de l'algorithme impacte directement les performances. Pour n=1000, O(n²) fait 1 million d'opérations vs O(n log n) qui n'en fait que 10 000." },
    { titre: "Structures de Données", texte: "Les structures de données organisent les informations efficacement :\n\n• Tableaux : accès O(1), insertion O(n)\n• Listes chaînées : insertion O(1), accès O(n)\n• Piles (LIFO) : push/pop O(1)\n• Files (FIFO) : enqueue/dequeue O(1)\n• Arbres binaires : recherche O(log n)\n• Tables de hachage : accès moyen O(1)\n\nChoisir la bonne structure est crucial : une liste chaînée est idéale pour les insertions fréquentes, un tableau pour les accès aléatoires." }
  ]},
  "Apprendre Python 3": { chapitres: [
    { titre: "Introduction à Python", texte: "Python est un langage interprété, dynamiquement typé, créé par Guido van Rossum en 1991. Sa philosophie : la lisibilité du code prime.\n\nPourquoi Python ?\n• Syntaxe claire et proche du langage naturel\n• Polyvalent : web, IA, data science, scripts\n• Grande communauté et bibliothèques riches\n• Idéal pour les débutants\n\nPremier programme :\n```python\nprint('Bonjour le monde!')\nnom = input('Votre nom : ')\nprint(f'Bienvenue, {nom}!')\n```" },
    { titre: "Variables et Types", texte: "Python est dynamiquement typé : le type est inféré automatiquement.\n\nTypes principaux :\n```python\nx = 42          # int\ny = 3.14        # float\ns = 'Bonjour'  # str\nb = True        # bool\nl = [1,2,3]    # list\nd = {'a': 1}   # dict\nt = (1,2)      # tuple\n```\n\nConversion de types :\n```python\nint('42')    # -> 42\nstr(3.14)    # -> '3.14'\nfloat('1.5') # -> 1.5\n```" },
    { titre: "Fonctions et Modules", texte: "Les fonctions en Python se définissent avec `def` :\n\n```python\ndef calculer_moyenne(notes):\n    if not notes:\n        return 0\n    return sum(notes) / len(notes)\n\nmoyenne = calculer_moyenne([12, 15, 18, 9])\nprint(f'Moyenne : {moyenne}')  # 13.5\n```\n\nModules : regroupent des fonctions réutilisables.\n```python\nimport math\nprint(math.sqrt(16))  # 4.0\n\nfrom datetime import datetime\nprint(datetime.now())\n```" }
  ]},
  "Git Pro (Français)": { chapitres: [
    { titre: "Les Bases de Git", texte: "Git est un système de contrôle de version distribué créé par Linus Torvalds.\n\nCommandes fondamentales :\n```bash\ngit init          # Initialiser un dépôt\ngit add .         # Indexer les fichiers\ngit commit -m 'message'  # Créer un commit\ngit status        # Voir l'état\ngit log           # Historique des commits\n```\n\nLe workflow Git :\n1. Modifier des fichiers\n2. Indexer avec git add\n3. Valider avec git commit\n4. Pousser avec git push" },
    { titre: "Branches et Fusion", texte: "Les branches permettent de travailler en parallèle sans perturber la branche principale.\n\n```bash\ngit branch feature/login  # Créer une branche\ngit checkout feature/login # Basculer\ngit merge feature/login   # Fusionner\ngit branch -d feature/login # Supprimer\n```\n\nStratégies de fusion :\n• Fast-forward : pas de commit de merge\n• Merge commit : conserve l'historique\n• Rebase : historique linéaire\n\nConflits : surviennent quand deux branches modifient la même ligne. Git les marque avec <<<<<<< et >>>>>>> pour résolution manuelle." }
  ]},
  "Eloquent JavaScript": { chapitres: [
    { titre: "Values, Types, and Operators", texte: "JavaScript has six primitive types: Number, String, Boolean, undefined, null, and Symbol.\n\nNumbers:\n```js\nconsole.log(3 / 2);    // 1.5\nconsole.log(0.1 + 0.2); // 0.30000000000000004\nNumber.isInteger(1.0);  // true\n```\n\nStrings:\n```js\nlet greeting = `Hello, ${'World'}!`;\ngreeting.length;      // 13\ngreeting.toUpperCase(); // HELLO, WORLD!\n```\n\nBoolean logic: && (and), || (or), ! (not) follow short-circuit evaluation." },
    { titre: "Functions", texte: "Functions are first-class values in JavaScript — they can be assigned, passed, and returned.\n\n```js\n// Declaration\nfunction square(x) { return x * x; }\n\n// Expression\nconst cube = function(x) { return x ** 3; };\n\n// Arrow function\nconst double = x => x * 2;\n\n// Closure\nfunction makeCounter() {\n  let count = 0;\n  return () => ++count;\n}\nconst counter = makeCounter();\ncounter(); // 1\ncounter(); // 2\n```" }
  ]},
  "Think Python 2e": { chapitres: [
    { titre: "The Way of the Program", texte: "The goal is to think like a computer scientist: combining formal language precision with creative problem-solving.\n\nPrograms are made of:\n1. Input — get data from keyboard, file, network\n2. Output — display data on screen or file\n3. Math — arithmetic operations\n4. Conditional — check conditions and execute accordingly\n5. Repetition — perform actions repeatedly\n\n```python\n# Every Python program starts simply:\nprint('Hello, World!')\n```\n\nPython is an interpreted language: the interpreter reads and executes programs line by line." },
    { titre: "Variables and Statements", texte: "A variable is a name that refers to a value.\n\n```python\nmessage = 'Hello'\nn = 17\npi = 3.141592653589793\n\ntype(message)  # <class 'str'>\ntype(n)        # <class 'int'>\ntype(pi)       # <class 'float'>\n```\n\nStatements are units of code that Python can execute. An expression is a combination of values, variables and operators.\n\nOperators: +, -, *, /, //, %, **\n```python\n17 // 3  # 5 (floor division)\n17 % 3   # 2 (modulo)\n2 ** 8   # 256 (power)\n```" }
  ]},
  "OS: Three Easy Pieces": { chapitres: [
    { titre: "Virtualization: The Process", texte: "A process is a running program. The OS creates the illusion of many CPUs through time-sharing.\n\nProcess states:\n• Running: executing on the CPU\n• Ready: waiting to be scheduled\n• Blocked: waiting for I/O\n\nProcess Control Block (PCB) stores:\n- Register context\n- Memory mappings\n- Open files\n- Scheduling info\n\nContext switching: saving and restoring CPU state when switching between processes. This is the fundamental mechanism enabling multitasking." },
    { titre: "Memory Management", texte: "The OS virtualizes memory, giving each process the illusion of a large private address space.\n\nKey mechanisms:\n1. Base and Bounds: simple hardware support\n2. Segmentation: multiple base/bounds pairs\n3. Paging: fixed-size memory chunks\n\nVirtual address = Virtual Page Number + Offset\nPhysical address = Physical Frame Number + Offset\n\nTLB (Translation Lookaside Buffer): hardware cache for address translations, crucial for performance.\n\nPage replacement policies: FIFO, LRU, Clock algorithm." }
  ]},
  "Clean Code": { chapitres: [
    { titre: "Meaningful Names", texte: "Names should reveal intent. Ask yourself: does the name tell you why it exists, what it does, and how it is used?\n\nBad vs Good:\n```java\n// Bad\nint d; // elapsed time in days\nList<int[]> getThem();\n\n// Good\nint elapsedTimeInDays;\nList<Cell> getFlaggedCells();\n```\n\nRules:\n• Use pronounceable names: genymdhms → generationTimestamp\n• Avoid encodings: phoneString → phone\n• Pick one word per concept: fetch/retrieve/get — choose one\n• Use domain names: AccountVisitor, JobQueue" },
    { titre: "Functions", texte: "Functions should be SMALL — ideally 4-6 lines.\n\nDo ONE thing. If you can extract another function with a different name, your function does more than one thing.\n\n```java\n// Bad: 3 levels of abstraction\npublic void processPayment(Order order) {\n  if (order.getItems() != null) {\n    for (Item i : order.getItems()) {\n      if (i.getPrice() > 0) { /* ... */ }\n    }\n  }\n}\n\n// Good: each function does one thing\npublic void processPayment(Order order) {\n  List<Item> validItems = getValidItems(order);\n  applyDiscounts(validItems);\n  chargeCustomer(validItems);\n}\n```\n\nArguments: fewer is better. 0 > 1 > 2 > 3. Avoid flag arguments." }
  ]},
  "The Clean Coder": { chapitres: [
    { titre: "Professionalism", texte: "A professional takes responsibility for their work and their career. When you make a mistake, you acknowledge it, fix it, and learn from it.\n\nKey principles:\n• Say 'No' when asked to do something impossible\n• Communicate honestly about timelines\n• Never compromise on quality\n• Keep your skills sharp\n\nThe professional commits to what they can deliver. Avoid saying 'I'll try' as a way to avoid commitment. Either you commit or you don't." },
    { titre: "Test Driven Development", texte: "TDD follows three laws:\n1. Write a failing test before any production code\n2. Write only enough test code to fail\n3. Write only enough production code to pass the test\n\nBenefits:\n• 100% code coverage\n• Instant defect detection\n• Decoupled, testable design\n• Living documentation\n\nTDD cycle: Red → Green → Refactor\n\nProfessionals practice TDD. Writing tests after the fact leads to incomplete coverage and tests that are written to pass, not to verify behavior." }
  ]},
  "SICP": { chapitres: [
    { titre: "Building Abstractions with Procedures", texte: "Computational processes are abstract beings that inhabit computers. Programs are like magical spells to control these processes.\n\nLisp (Scheme) example:\n```scheme\n(define (square x) (* x x))\n(define (sum-of-squares x y)\n  (+ (square x) (square y)))\n(sum-of-squares 3 4) ; => 25\n```\n\nSubstitution model: to evaluate a procedure application, evaluate each argument, then substitute them into the procedure body.\n\nRecursion vs Iteration:\n```scheme\n; Recursive (builds call stack)\n(define (factorial n)\n  (if (= n 1) 1 (* n (factorial (- n 1)))))\n\n; Iterative (tail recursive)\n(define (factorial n acc)\n  (if (= n 0) acc (factorial (- n 1) (* n acc))))\n```" }
  ]},
  "Bases de Données": { chapitres: [
    { titre: "Le Modèle Relationnel", texte: "Le modèle relationnel organise les données en tables (relations). Chaque table a des colonnes (attributs) et des lignes (tuples).\n\nConcepts clés :\n• Clé primaire : identifiant unique d'un tuple\n• Clé étrangère : référence vers une clé primaire d'une autre table\n• Intégrité référentielle : cohérence entre tables liées\n\nNormalisation (éviter la redondance) :\n1. 1NF : attributs atomiques\n2. 2NF : pas de dépendances partielles\n3. 3NF : pas de dépendances transitives\n\nExemple :\n```\nÉtudiant(id, nom, email)\nCours(id, titre, credits)\nInscription(etudiant_id, cours_id, date)\n```" },
    { titre: "SQL : Langage de Requêtes", texte: "SQL (Structured Query Language) manipule les bases de données relationnelles.\n\n```sql\n-- Sélection\nSELECT nom, email FROM Etudiant WHERE age > 18;\n\n-- Jointure\nSELECT e.nom, c.titre\nFROM Etudiant e\nJOIN Inscription i ON e.id = i.etudiant_id\nJOIN Cours c ON i.cours_id = c.id;\n\n-- Agrégation\nSELECT cours_id, COUNT(*) as inscrits\nFROM Inscription\nGROUP BY cours_id\nHAVING COUNT(*) > 10;\n\n-- Index (performance)\nCREATE INDEX idx_email ON Etudiant(email);\n```" }
  ]},
  "Intelligence Artificielle": { chapitres: [
    { titre: "Introduction à l'IA", texte: "L'Intelligence Artificielle vise à créer des systèmes capables de raisonner, apprendre et résoudre des problèmes complexes.\n\nDomaines principaux :\n• Recherche heuristique (A*, minimax)\n• Représentation des connaissances\n• Apprentissage automatique (Machine Learning)\n• Traitement du langage naturel (NLP)\n• Vision par ordinateur\n\nApproches :\n1. IA symbolique : règles explicites, logique formelle\n2. IA connexionniste : réseaux de neurones, apprentissage\n3. IA hybride : combine les deux\n\nTest de Turing : un programme passe le test si un humain ne peut pas distinguer ses réponses de celles d'un autre humain." },
    { titre: "Algorithmes de Recherche", texte: "La recherche dans un espace d'états est fondamentale en IA.\n\nRecherche non informée :\n• BFS (largeur) : optimal, mémoire O(b^d)\n• DFS (profondeur) : mémoire O(bd)\n• Dijkstra : chemins les plus courts\n\nRecherche informée (heuristiques) :\n• A* : f(n) = g(n) + h(n), optimal si h admissible\n\nJeux à deux joueurs :\n```\nMinimax : max(min(résultats adversaire))\nAlpha-Beta : élagage pour éviter les branches inutiles\n```\n\nApplication : IA d'échecs, résolution de puzzles, navigation GPS." }
  ]},
  "Cybersécurité Moderne": { chapitres: [
    { titre: "Principes de Sécurité", texte: "La sécurité informatique repose sur la triade CIA :\n• Confidentialité : seules les personnes autorisées accèdent aux données\n• Intégrité : les données ne sont pas altérées\n• Disponibilité : les services restent accessibles\n\nPrincipes fondamentaux :\n1. Moindre privilège : accorder le minimum de droits nécessaires\n2. Défense en profondeur : plusieurs couches de sécurité\n3. Fail secure : en cas d'échec, refuser l'accès\n4. Séparation des duties : aucune personne seule ne contrôle tout\n\nSurface d'attaque : ensemble des points d'entrée potentiels d'un système. La réduire diminue les risques." },
    { titre: "Cryptographie", texte: "La cryptographie protège les informations par transformation mathématique.\n\nChiffrement symétrique :\n• AES-256 : standard actuel, même clé pour chiffrer/déchiffrer\n• Vitesse : très rapide (matériel dédié)\n• Problème : distribution de la clé\n\nChiffrement asymétrique :\n• RSA, ECDSA : paire clé publique/privée\n• Lent mais résout le problème de distribution\n• Base de HTTPS, signatures numériques\n\nHachage :\n• SHA-256, bcrypt : irréversible\n• Utilisation : mots de passe, intégrité des fichiers\n\n```\nHTTPS = HTTP + TLS (Transport Layer Security)\nHandshake : RSA pour échanger clé AES, puis AES pour les données\n```" }
  ]},
  "Deep Learning": { chapitres: [
    { titre: "Neural Networks Basics", texte: "A neural network is inspired by the brain's structure. It consists of layers of interconnected neurons.\n\nArchitecture:\n• Input layer: receives raw data\n• Hidden layers: learn representations\n• Output layer: produces predictions\n\nNeuron computation:\n```\noutput = activation(weights · inputs + bias)\n```\n\nActivation functions:\n• Sigmoid: σ(x) = 1/(1+e^-x) — outputs 0-1\n• ReLU: max(0,x) — most common today\n• Softmax: for multi-class output\n\nBackpropagation: compute gradients using chain rule, update weights with gradient descent." },
    { titre: "Training Deep Networks", texte: "Training adjusts weights to minimize loss.\n\nLoss functions:\n• MSE for regression: L = Σ(y-ŷ)²/n\n• Cross-entropy for classification: L = -Σy·log(ŷ)\n\nOptimizers:\n• SGD: update w = w - lr·∇L\n• Adam: adaptive learning rates\n\nRegularization (prevent overfitting):\n• Dropout: randomly zero neurons during training\n• L2: penalize large weights\n• Batch normalization: normalize layer inputs\n\nHyperparameters to tune:\n• Learning rate (most critical)\n• Batch size\n• Network depth/width\n• Epochs" }
  ]},
  "Linux : Le système": { chapitres: [
    { titre: "Le Noyau Linux", texte: "Linux est un système d'exploitation open-source basé sur Unix. Le noyau gère le matériel et fournit des services aux applications.\n\nArchitecture :\n• Noyau monolithique modulaire\n• Gestion des processus (scheduler CFS)\n• Gestion mémoire (pages, swap)\n• Système de fichiers (ext4, btrfs, xfs)\n• Réseau (pile TCP/IP)\n\nCommandes essentielles :\n```bash\nls -la          # Lister fichiers\ncd /chemin      # Changer répertoire\nchmod 755 file  # Permissions\nps aux          # Processus actifs\nkill -9 PID     # Terminer processus\ndf -h           # Espace disque\ntop             # Moniteur système\n```" },
    { titre: "Shell et Scripting", texte: "Le shell Bash est l'interpréteur de commandes standard.\n\n```bash\n#!/bin/bash\n# Script de sauvegarde\n\nSRC_DIR='/home/user/documents'\nBACKUP_DIR='/backup'\nDATE=$(date +%Y%m%d)\n\n# Créer sauvegarde\ntar -czf \"$BACKUP_DIR/backup_$DATE.tar.gz\" \"$SRC_DIR\"\n\n# Vérification\nif [ $? -eq 0 ]; then\n  echo 'Sauvegarde réussie'\nelse\n  echo 'Erreur lors de la sauvegarde' >&2\n  exit 1\nfi\n```\n\nVariables, boucles, conditions, fonctions — Bash permet d'automatiser toutes les tâches système répétitives." }
  ]},
  "JavaScript: The Good Parts": { chapitres: [
    { titre: "The Good Parts", texte: "JavaScript has good parts and terrible parts. The key is to focus on the good ones.\n\nGood parts include:\n• Functions as first-class objects\n• Dynamic objects with prototypal inheritance\n• Object literals and array literals\n• Closures\n\nParts to avoid:\n• Global variables — pollute the namespace\n• == (prefer ===)\n• with statement\n• eval\n• continue statement\n\n```js\n// Good: IIFE to avoid global pollution\n(function() {\n  'use strict';\n  var myModule = {};\n  // your code here\n}());\n```" },
    { titre: "Closures and Modules", texte: "A closure gives a function access to the scope in which it was defined, even after that scope has closed.\n\n```js\nfunction makeAdder(x) {\n  return function(y) {\n    return x + y;  // x is 'closed over'\n  };\n}\nvar add5 = makeAdder(5);\nadd5(3); // 8\nadd5(10); // 15\n\n// Module pattern\nvar module = (function() {\n  var private = 0;\n  return {\n    increment: function() { private++; },\n    value: function() { return private; }\n  };\n}());\nmodule.increment();\nmodule.value(); // 1\n```" }
  ]},
  "Maîtriser React": { chapitres: [
    { titre: "Composants et Props", texte: "React décompose l'UI en composants réutilisables. Chaque composant est une fonction qui retourne du JSX.\n\n```jsx\n// Composant fonctionnel\nfunction CarteCours({ titre, instructeur, progression }) {\n  return (\n    <div className='card'>\n      <h2>{titre}</h2>\n      <p>Par {instructeur}</p>\n      <div className='progress'>\n        <div style={{ width: `${progression}%` }} />\n      </div>\n    </div>\n  );\n}\n\n// Utilisation\n<CarteCours\n  titre='React Avancé'\n  instructeur='Alice'\n  progression={75}\n/>\n```\n\nLes props sont en lecture seule — un composant ne doit jamais modifier ses props." },
    { titre: "State et Hooks", texte: "Les hooks permettent d'utiliser l'état et le cycle de vie dans les composants fonctionnels.\n\n```jsx\nimport { useState, useEffect, useCallback } from 'react';\n\nfunction Minuteur() {\n  const [secondes, setSecondes] = useState(0);\n  const [actif, setActif] = useState(false);\n\n  useEffect(() => {\n    if (!actif) return;\n    const id = setInterval(() => {\n      setSecondes(s => s + 1);\n    }, 1000);\n    return () => clearInterval(id); // cleanup\n  }, [actif]);\n\n  return (\n    <div>\n      <p>{secondes}s</p>\n      <button onClick={() => setActif(a => !a)}>\n        {actif ? 'Pause' : 'Démarrer'}\n      </button>\n    </div>\n  );\n}\n```" }
  ]},
};

// Fallback générique pour les livres sans contenu spécifique
function contenuGenerique(titre: string): ContenuLivre {
  return {
    chapitres: [
      {
        titre: `Introduction à ${titre}`,
        texte: `${titre} est un ouvrage de référence dans son domaine. Ce livre couvre les fondamentaux théoriques et pratiques essentiels pour comprendre le sujet en profondeur.\n\nLes concepts clés abordés :\n• Fondements théoriques et définitions essentielles\n• Méthodes et techniques pratiques éprouvées\n• Exemples concrets issus de cas réels\n• Bonnes pratiques et recommandations d'experts\n\nCe contenu est conçu pour accompagner votre lecture du livre original disponible en téléchargement.`
      },
      {
        titre: "Concepts Fondamentaux",
        texte: `Ce chapitre explore les bases indispensables pour maîtriser ${titre}.\n\nPour bien comprendre ce domaine, il faut assimiler plusieurs niveaux de complexité :\n\n1. Niveau débutant : comprendre la terminologie et les concepts de base\n2. Niveau intermédiaire : appliquer les techniques aux problèmes courants\n3. Niveau avancé : optimiser, créer et innover\n\nConseils d'apprentissage :\n• Pratiquez régulièrement avec des exercices\n• Lisez le code source de projets réels\n• Participez à des projets collaboratifs\n• Documentez votre progression\n\nLa maîtrise vient avec la pratique constante et la curiosité intellectuelle.`
      }
    ]
  };
}

export function getContenuLivre(titre: string): ContenuLivre {
  const normalize = (s: string) => s.toLowerCase().trim();
  const key = Object.keys(C).find(k => normalize(k) === normalize(titre));
  return key ? C[key] : contenuGenerique(titre);
}
