// Commentaires explicatifs en français :
// Ce fichier contient les listes de valeurs fixes (énumérations) utilisées dans l'application.

export enum Role {
  STUDENT = 'etudiant',
  ADMIN = 'admin',
}

export enum LearningMode {
  GUIDED = 'guide',
  FREE = 'libre',
}